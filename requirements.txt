# Core dependencies
yt-dlp>=2026.07.04
openai-whisper>=20231117
easyocr>=1.7.0
Pillow>=10.0.0
moviepy>=1.0.3
instaloader>=4.10
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0

# PornHub / impersonation support (required for sites that block bots)
curl_cffi>=0.15.0

# Gemini script generator
google-genai>=2.0.0
# Service-account auth for Gemini (Case Commentary / Script Studio tabs).
# REQUIRED — without these the AI tabs throw "google-auth library required".
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# PyTorch is installed SEPARATELY — use CPU-only index:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# Do NOT pin exact version here (breaks on different Python/PyTorch versions).
